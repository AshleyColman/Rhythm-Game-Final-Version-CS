namespace Enums
{
    public enum Difficulty { TwoKey, FourKey, SixKey };

    public enum HitobjectType { Hitobject0 };

    public enum Grade { S_PLUS, S, A_PLUS, A, B_PLUS, B, C_PLUS, C, D_PLUS, D, E_PLUS, E, F_PLUS, F, X_PLUS, X };

    public enum NotificationType { Error, General, TwoKey, FourKey, SixKey };
}
